import { GenotypeOrderByPipe } from './genotype-order-by.pipe';

describe('GenotypeOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new GenotypeOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
