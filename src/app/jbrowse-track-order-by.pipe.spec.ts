import { JbrowseTrackOrderByPipe } from './jbrowse-track-order-by.pipe';

describe('JbrowseTrackOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new JbrowseTrackOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
