import { AnnotationTableFilterPipe } from './annotation-table-filter.pipe';

describe('AnnotationTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnotationTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
